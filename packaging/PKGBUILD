pkgname=OurTube
pkgver=1.0
pkgrel=1
pkgdesc="GUI for downloading youtube videos. Using yt-dlp."
arch=('any')
license=('MIT')
source=("git+https://github.com/Samuraien2/OurTube.git")
sha256sums=('SKIP')
depends=('qt6-base' 'yt-dlp' 'ffmpeg')

build() {
    make
}
